import React from "react";
import { getItems } from '../../helpers/storage';

import "./styles.css";

const Cart = () => {
    console.log(getItems())
  return (
    <div className="cart">
      
    </div>
  );
};

export default Cart;
